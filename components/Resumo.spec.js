// import Resumo from './Resumo.js';
// import { mock2 } from '../test/mocks.js';
// import { farejaDetalhesCard } from '../API.js';

// jest.mock('../API.js', () => ({
//     farejaDetalhesCard: jest.fn(() => Promise.resolve(mock2)), 
// }));

// describe('Resumo', () => {
//   it('Deve renderizar 1 card', async () => {
  
//     Resumo();

//     expect(farejaDetalhesCard).toHaveBeenCalledTimes(1);
//   });
// }); 